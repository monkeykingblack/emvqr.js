image: node:latest

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - cat "${CONFIG_PRIVATE_REGISTRY}" > ~/.yarnrc.yml
    - cat ~/.yarnrc.yml
    - yarn install --immutable
    - yarn test
    - yarn release
  only:
    - main
  tags:
    - runner-cloud